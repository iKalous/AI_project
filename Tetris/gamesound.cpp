#include "gamesound.h"
#include <cstdio>

void drop_piece() { Beep(doo,ban); }

void clean_piece() { Beep(mi,ban); }

void clean_line() { Beep(mi1,ban); }

void game_over() { Beep(qla,ban); }

void break_the_record() { Beep(la1,ban); }

void start_the_game() { Beep(do1,ban); }

void game_is_over() { Beep(qdo,ban); }

void win() { Beep(so1,pai); }